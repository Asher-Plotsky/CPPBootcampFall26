#include "Shape.h"

// Nothing to define here: every function in Shape is pure virtual
// (that "= 0" in the header). This .cpp file exists mostly so the
// project structure stays consistent, and it's where you'd put any
// shared, non-pure helper behavior if Shape ever needed one.
